# graspologic, used by src/kg/graphrag/community.py for hierarchical Leiden community
# detection (the port falls back to Louvain when it is missing). graspologic 3.4.4
# declares numpy<2, but the research environment ran it with numpy 2.2.6 (imports and
# partitioning work), so it cannot be resolved together with requirements.txt.
# Install exactly as the research environment had it:
#   pip install --no-deps -r requirements-graspologic.txt
graspologic==3.4.4
graspologic-native==1.2.5
POT==0.9.6.post1
anytree==2.13.0
beartype==0.18.5
future==1.0.0
gensim==4.4.0
hyppo==0.5.2
joblib==1.5.3
matplotlib==3.10.8
scikit-learn==1.8.0
scipy==1.17.1
seaborn==0.13.2
statsmodels==0.14.6
typing-extensions==4.15.0
umap-learn==0.5.9.post2
