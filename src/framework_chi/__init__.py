"""{framework}^χ — adaptive cascade for biomedical QA.

Public surface is intentionally narrow: the V14CascadeClient implements
the framework-eval QAClient protocol and is registered as the
`v14-cascade-dual-rerank-grounded` entry point in pyproject.toml.
"""

__version__ = "0.1.0"
